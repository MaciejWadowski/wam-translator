% compiler: GNU Prolog 1.4.5
% file    : test.pl


file_name('/mnt/nvme/Programming/tkk/test.pl').


predicate(user:man/1,1,static,private,monofile,global,[
    switch_on_term(2,1,fail,fail,fail),

label(1),
    switch_on_atom([(jacek,3),(wojtek,5)]),

label(2),
    try_me_else(4),

label(3),
    get_atom(jacek,0),
    proceed,

label(4),
    trust_me_else_fail,

label(5),
    get_atom(wojtek,0),
    proceed]).


predicate(user:woman/1,3,static,private,monofile,global,[
    switch_on_term(2,1,fail,fail,fail),

label(1),
    switch_on_atom([(agata,3),(agnieszka,5)]),

label(2),
    try_me_else(4),

label(3),
    get_atom(agata,0),
    proceed,

label(4),
    trust_me_else_fail,

label(5),
    get_atom(agnieszka,0),
    proceed]).


predicate(user:parent/2,5,static,private,monofile,global,[
    switch_on_term(4,1,fail,fail,fail),

label(1),
    switch_on_atom([(agata,2),(wojtek,3)]),

label(2),
    try(5),
    trust(9),

label(3),
    try(7),
    trust(11),

label(4),
    try_me_else(6),

label(5),
    get_atom(agata,0),
    get_atom(jacek,1),
    proceed,

label(6),
    retry_me_else(8),

label(7),
    get_atom(wojtek,0),
    get_atom(jacek,1),
    proceed,

label(8),
    retry_me_else(10),

label(9),
    get_atom(agata,0),
    get_atom(agnieszka,1),
    proceed,

label(10),
    trust_me_else_fail,

label(11),
    get_atom(wojtek,0),
    get_atom(agnieszka,1),
    proceed]).
